# 更新日志

## v1.2.5（预览测试版）

v1.2.5 是一次「使用逻辑 + 小功能」的集中升级：大幅改良画布操作与排序 / 整理体验（画布缩放平移、便签框选、分组折叠、最近分组、右键快捷插入、指针拖拽排序），并补齐撤销重做、Markdown 预览、便签归档、提醒稍后再响、标签建议等实用能力；同时把快捷键全面纳入设置面板、统一亚克力透明下的顶栏控件可读性。

### 画布与使用逻辑
- **画布缩放 / 平移**：`Ctrl+滚轮`以光标为锚缩放、`空格或鼠标中键`拖动平移，右下角工具栏一键恢复 100%，缩放值持久保存
- **便签框选**：在空白处拖框即可多选，配合**批量删除 / 移动分组 / 整组拖动**，比逐张点选更高效
- **分组折叠 / 展开**：折叠可把整组便签收起来，取消折叠精确恢复到折叠前布局（快照式可逆，不跑位、不重叠）
- **最近使用分组**：常用分组自动置顶，快速切换
- **空白画布右键快捷插入**：新建便签 / 新建待办 / 粘贴文本为新便签 / 一键整理
- **备忘录 / 文档 / 排序面板指针拖拽排序**：改用指针事件替代原生 HTML5 拖拽，**拖动时仍可用滚轮滚动**画布（可边拖边滚），并按指针位置实时显示落点指示
- **批量选中只选不编辑**：批量模式下点击便签 / 备忘录内容区只切换选中、不再误入编辑，退出批量后恢复正常编辑
- **分组芯片左右滚动箭头**：分组过多时可点箭头或滚轮左右翻看；画布滚动条随内容自适应

### 排序与整理
- **一键整理**：精确恢复「保存当前排序」时记录的布局（顺序 + 位置）；未保存过时按当前排序模式紧凑排列
- **保存当前排序 ↔ 一键整理 联动**：「保存当前排序」记录自定义顺序 + 布局快照；「一键整理」恢复到该快照；两键只在便签视图显示

### 小功能池
- **撤销 / 重做完善**：新建、删除、移动、缩放、分组等结构操作均可撤销重做（`Ctrl+Z` / `Ctrl+Shift+Z`），并纳入全局设置改键
- **Markdown 预览**：便签与文档可切换为只读预览，直接看到加粗、高亮、图片、表格等渲染效果
- **便签归档 / 置灰**：归档便签移出常规视图，提供独立「归档」入口查看与恢复，归档卡自动降饱和置灰
- **提醒稍后再响**：闹铃可延后 5 / 10 / 30 分钟再次提醒
- **标签建议**：在「添加到分组」中按笔记内容自动推荐最合适的分组

### 快捷键与外观
- **快捷键系统**：全局唤起 / 新建 + 编辑器（加粗 / 高亮 / 对齐 / 复制图片）快捷键，均可在设置内改键与恢复默认；新增「应用」级撤销 / 重做
- **便签外观自定义**：圆角 / 阴影 / 边框粗细与颜色 / 字体字距，实时作用于所有便签卡片
- **右键菜单亚克力 + 透明度**：右键菜单可开启磨砂玻璃并调节透明度，随主题统一
- **主题化关闭确认弹窗**：与顶栏配色、透明度、亚克力一致；窗口尺寸与最大化状态跨重启记忆
- **顶栏控件可读性**：亚克力 / 透明 / 背景图下，顶栏图标按钮与分组箭头统一为与「分组芯片 / 批量按钮」一致的磨砂胶囊 + 细边框，清晰美观

### 修复
- 修复待办便签下「预览」按钮 `null.onclick` 导致的初始化崩溃（画布空白、按钮无响应、窗口无法关闭）
- 修复空白分组下新建便签后回到「全部」视图与其他便签重叠的问题（`positionAll` 独立取「全部」空位）
- 修复安装后无法启动：`main.js` 在打包环境仍 `require('./ipc-contract.js')`（开发专用模块，未随包分发）导致 `Cannot find module` —— 改为仅开发模式 / 显式开启时惰性加载
- 构建 / 版本：安装包与关于页版本号统一为 `1.2.5-preview`

## v1.2.4

v1.2.4 是一次「效率 + 体验 + 细节」的更新：新增文本对齐与批量选中两大效率功能，恢复 Windows 原生贴靠，并修复多项 Win11 相关与回归问题，同时打磨复杂背景下的可读性、性能与工具栏交互。

### 体验与外观
- **文本对齐**：便签卡片右键菜单、文档模式工具栏、钉桌面便签右键菜单加入**左 / 居中 / 右对齐**，快捷键 `Ctrl+L` / `Ctrl+E` / `Ctrl+R`；对齐状态随内容持久保存，切换视图不丢失。对齐改为整块（段落）对齐而非 `execCommand`，避免杂散样式与误高亮；**图片支持独立对齐**（点选某张图片后对齐，居中/左/右），修复对齐后残留 `[` / `[[` 字符、"多段只对齐一段"、图片不对齐、以及删除按钮(✕)被拉伸到边角的问题
- **批量选中**：顶栏「批量选中」进入多选，点击便签切换选中；底部悬浮操作栏支持**全选 / 移动分组 / 批量删除**，且可**拖动任意已选便签整组一起移动**；批量栏跟随顶栏主题（颜色/透明度/亚克力统一），任何背景下清晰可读
- **一键整理更紧凑**：改用按便签实际宽高的行式紧凑打包（间距恒定 18px），尺寸不齐的便签也不再被拉出远距离
- **桌面玻璃拟态修复**：钉在桌面的便签此前丢失模糊磨砂感，现补回背景模糊（`backdrop-filter`）与 Windows 原生亚克力材质，恢复与应用内一致的效果
- **背景可读性增强**：选择明亮或较复杂的背景图后，自动提升界面与便签文字对比；**保留背景原亮度与色彩**（不整图压暗），避免「为了可读牺牲美观」
- **文字清晰**：深色便签文字保持锐利，仅浅色文字自动加投影；空态提示（无便签时）改为「字幕式」亮字 + 柔投影，图标提亮，任何背景下都清楚自然
- **工具条改进**：便签 / 备忘 / 文档工具条顺序统一（「钉在桌面」在前、「置顶」在删除前），图标放大更清晰
- **视图切换**：去掉生硬的灰色外框，改为浅淡底色，亮色背景下更柔和
- **窗口置顶 / 钉在桌面图标**：调整并统一为更清晰的图钉样式

### 性能
- **画布增量渲染**：只在「进入画布视图」时全量重建，平时编辑 / 切换置顶 / 改色等只更新受影响的那张便签，其余便签的 DOM 与选中 / 焦点保持不变，多便签时更流畅

### 修复
- **便签粘贴图片失效**：主进程在先前重构中丢失了媒体 / 剪贴板相关 IPC 处理器与自定义协议（`note:save-image`、`note:add-image-file`、`clipboard:read-image`、`clipboard:read-files`、`note-img://` 等），导致便签内无法粘贴或插入图片。现已全数补回（含字体/声音、备份导出、媒体清理、自动更新、开机自启等一并恢复）
- **贴靠（snap）恢复**：窗口此前用透明（`transparent`）导致 Windows 原生贴靠失效。现改用 `titleBarStyle: 'hidden'` + `titleBarOverlay`（官方推荐做法，与 VS Code 相同），恢复完整的原生窗口控制与 Windows 11 贴靠布局（拖到顶部最大化、左右半屏、四角四分屏，并支持系统贴靠布局选择），同时修复拖到顶部后无法还原的问题
- **Win11 拖动便签卡顿**：拖动期间对被拖便签启用 `will-change`、对未被拖动的便签关闭指针命中（`pointer-events:none`）并暂时关闭过渡动画，避免光标扫过时兄弟卡片 hover 重绘导致的卡顿（Win11 23H2 更明显）；保留玻璃磨砂效果，不再出现拖动时变透明
- **便签卡**：选中文本后右键即可左/居中/右对齐，状态持久

### 测试与工程
- 扩充 e2e：英文语言渲染、钉窗深度编辑（改标题数据同步）、多便签（预置 80 张可渲染可编辑）、画布增量渲染回归、编辑器 Ctrl+B 加粗安全网
- 新增外观可读性相关的单元测试（亮度 / WCAG 对比度），并校验「设置背景图自动启用可读性、清除后取消」
- 修复 e2e 数据隔离与版本号注入：钉窗 / 主窗补回 `userData` 隔离与 `--app-version` 注入，测试不再读到真实数据

## v1.2.3

v1.2.3 是一次「体验 + 性能 + 可靠性」的综合更新：启动更快、运行更省资源，并把多处重复实现收归单一来源。

### 新增
- **开机自启动**：全局设置「数据」页新增「系统 → 开机时自动启动便签」，默认关闭，可在登录 Windows 后自动拉起应用
- **自动化测试网**：引入 Playwright e2e，覆盖真实 Electron 链路的启动 / 编辑 / 钉窗 / 置顶高亮 / 玻璃拟态 / 渲染缓存等，与原有 `npm test` 单测并行

### 修复
- **备份与清理不一致**：之前备份未打包「回收站」里便签引用的图片/字体/声音，跨设备恢复后回收站「断图」，而清理却删不掉遗留孤儿；现统一引用集合，回收站媒体一并处理
- **版本号双源**：关于页 / 更新提示与安装包版本可能不一致；现由 `app.getVersion()` 单一注入
- **样式注入**：`[[c:颜色]]` 与表格边框/字体样式值经导入数据可注入 CSS；新增 `sanitizeCss` 清洗，仅放行合法字符
- **外链安全**：打开链接仅放行 http/https，阻断 `file://` 等协议注入
- **视觉反馈**：顶栏「窗口置顶」按钮点击后无提示 → 激活态高亮；文档模式顶部工具条在复杂背景/背景图下看不清 → 磨砂横幅
- **空指针防护**：窗口销毁后托盘「新建便签 / 置顶」不再崩溃
- **故障可查**：数据读写 / 备份 / 还原关键路径失败不再静默，输出错误日志

### 优化（体验 & 性能）
- **启动提速**：主窗口最先创建并显示，托盘 / 提醒 / 钉窗 / 自动更新 / 全局快捷键延后初始化
- **读写提速**：主进程内存数据缓存 + 防抖落盘，读取不再反复全量读盘，写盘批量合并（保留原子写与 `.bak` 回退）
- **后台省电**：钉窗 `always-on-top` 去掉 2 秒轮询；主窗口隐藏时启用渲染节流
- **渲染提速**：富文本按内容指纹缓存，内容不变不再重复解析 Markdown
- **视觉降耗**：玻璃拟态降低 `backdrop-filter` 模糊成本

### 其他（架构 & 去重）
- 渲染函数（`formatInlineText` / 表格 / 富文本）与翻译表统一到 `logic.js`，主窗口与钉窗复用，消除重复实现
- 颜色 / 排序 / 引用清理 / Markdown 导出 / 媒体收集 / 剪贴板解析 / 自定义协议等收归单一来源，便于维护与测试

## v1.2.2

v1.2.2 主要修复复制粘贴问题、新增手动检查更新，并统一版本管理：

### 修复
- **便签内 Ctrl+C / Ctrl+V 无法复制粘贴**（含标题）：修复粘贴处理在 `await` 后剪贴板数据失效导致的粘贴无效，现在纯文本可正常粘贴，且保留文件粘贴为链接、图片粘贴为图片的行为
- **自动更新时提示「便签无法关闭」导致更新失败**：由于应用常驻系统托盘、关闭窗口后进程仍驻留，覆盖安装时 NSIS 检测到 exe 被占用而报错并回滚。现在「重启安装」时会先关闭所有窗口与托盘，确保进程干净退出后再安装
- 统一版本号来源，避免多处不一致

### 新增
- **「关于」页新增「检查更新」按钮**：用户可手动检测新版本，有更新时弹窗提示下载与重启安装
- 安装包统一命名为 ASCII（`MyNotes-Setup-版本号.exe`），本地与 GitHub 发布一致

### 其他
- 数据安全加固、自包含备份、模块化与测试、Markdown 导出、自动更新/签名链路等均在 v1.2.1 引入

## v1.2.1

v1.2.1 是一次综合性更新：在数据安全加固的基础上，进一步完成模块化、自动化测试、Markdown 导出与自动更新 / 签名链路的完善。

### 数据安全加固
- **原子写入**：数据改为「先写临时文件、再原子替换」，杜绝断电 / 崩溃导致 JSON 写一半损坏
- **版本号机制**：数据文件标注版本号 `version`，读取时自动迁移归一化，为未来大版本升级留好扩展点（导出 / 导入 / 备份均已标注）
- **损坏自动回退**：数据文件解析失败时自动回退到上一版备份 `.bak`，不再直接丢数据

### 备份自包含（跨设备迁移不断图）
- **一键备份**改为文件夹自包含包：`data.json` 自动携带 `images / backgrounds / fonts / sounds` 等实际引用的媒体文件
- **导入**自动还原备份里附带的媒体文件，迁移到新电脑后图片 / 字体 / 背景原样保留
- 只打包实际引用的媒体，缺失 / 孤儿文件自动跳过，备份更干净

### 模块化与自动化测试
- 抽取纯逻辑为单一来源模块 `logic.js`（颜色 / 转义 / 引用清理 / 排序 / Markdown 导出），主窗口与钉窗统一引用，消除重复
- 引入 Node 内置测试框架（无框架依赖）`npm test`，为排序、引用清理、Markdown 导出、颜色与转义等核心逻辑建立回归保障

### 导出 Markdown
- 新增 **Markdown 导出**：便签可导出为 `# 标题`、加粗、高亮、文件链接、表格、待办清单；图片以 base64 内嵌为**自包含单文件**，跨工具可用

### 自动更新与签名链路
- 接入 `electron-updater`：打包应用启动自动检查更新，弹窗询问下载，下载完询问重启安装
- 发布使用 **GitHub Releases**（`LucasRiver9527/Note-Memo`），亦可临时覆盖 / 禁用（`MYNOTES_UPDATE_URL`、`MYNOTES_DISABLE_AUTOUPDATE=1`）
- 代码签名链路：设置 `CSC_LINK` / `CSC_KEY_PASSWORD` 后自动签名，未提供证书时自动跳过

### 媒体健壮性
- 图片等媒体文件缺失时，显示「图片已丢失」友好占位提示，可一键移除，不再显示破图
- 中英双语提示
- 新增「**清理未引用媒体**」：一键删除不再被任何便签 / 回收站 / 设置引用的图片、字体、背景与音频文件（保留回收站引用，防误删可恢复数据）

### 其他
- 修复若干数据读取与备份相关的边界问题

## v1.2.0

v1.2 是便签的一次大版本更新，围绕外观、编辑体验与全新的表格功能进行全面升级：

### 窗口与贴靠
- 「贴靠」：窗口拖到屏幕边缘自动调整大小（顶部最大化、左右半屏、四角四分屏）
- 便签钉在桌面，支持 Windows 亚克力玻璃模糊效果
- 顶栏亚克力模糊，可自定义顶栏底色与透明度
- 右上角最小化 / 最大化 / 关闭按钮与顶栏样式统一
- 桌面便签右键菜单：集成常用功能，支持调节透明度

### 外观与主题
- 外观设置分模块：便签外观 / 主程序外观
- 12 套主题配色，薄荷（默认）置顶；恢复默认外观与默认主题一致
- 便签底色自定义，文字支持部分选取改色
- 高对比度按钮优化，开启亚克力模糊后仍清晰可读

### 编辑与内容
- 支持 Markdown 加粗 / 高亮（Ctrl+B / Ctrl+H）
- 编辑时 Ctrl + 鼠标滚轮快捷调整字体大小
- 图片按光标插入位置放置，支持拖入图片 / 文件
- 粘贴文件 / 文件夹路径自动识别：文件夹跳转、文件快速打开
- 支持导入自定义字体（.ttf / .otf / .woff）
- 便签内复制支持选取部分复制

### 视图
- 新增「文档」视图：像文档一样查看与编辑便签（支持 Markdown）
- 备忘录视图支持拖动调整上下顺序
- 待办区集中管理待办与时间提醒

### 表格（全新自研轻量表格）
- 插入 / 删除 / 合并 / 拆分单元格
- 右键表格设置：边框颜色、边框粗细、文字大小、文字颜色预设
- 斜线表头（两种方向，可编辑斜线两侧文字）
- 双击单元格编辑，支持表格内换行

### 提醒与数据
- 待办提醒闹铃声音，可自定义声音与音量，电脑待机时也能保证提醒
- 一键导出备份，回收站误删恢复与到期自动清理
- 新增「关于」页面：版本、作者、更新说明

### 修复
- 修复贴靠、拖动、透明对比度、表格编辑等多项问题

## v1.1.0

本次更新带来全新设置中心与多项实用功能：

- 全新设置中心：外观、字体、排序、备份、回收站、数据，一个面板全搞定
- 支持简体中文 / English 双语界面
- 便签可插入图片：粘贴、选图、拖拽调整大小
- 可导入自定义字体文件（.ttf / .otf / .woff）
- 新增「待办区」视图，集中管理待办与时间提醒
- 一键导出备份，可自选备份文件夹
- 回收站：误删可恢复，到期自动清理
- 一键整理便签，多种排序方式
